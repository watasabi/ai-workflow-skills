# Change: install script (archived context)

> Historical note: this change tracked adding an install script tied to a previous CI vendor. The current tree uses `install.sh` with **GitHub Releases** and neutral env vars (`AI_WORKFLOW_SKILLS_CATALOG`, `GITHUB_REPOSITORY`). Details of the old pipeline are intentionally omitted from this fork.

| Status   | Complete (superseded by neutral `install.sh`) |
|----------|-----------------------------------------------|
